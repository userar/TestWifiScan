<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="match_parent">
    
    <TextView
        android:id="@+id/hidden_network_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="24dp"
        android:textStyle="bold"
        android:text="Hidden network"/>

    <TextView
        android:id="@+id/bssid_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/hidden_network_title"
        android:layout_alignStart="@id/hidden_network_title"
        android:text="BSSID:"/>

    <TextView
        android:id="@+id/bssid_value"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@id/bssid_title"
        android:layout_toEndOf="@id/bssid_title"
        android:layout_marginStart="12dp"
        android:text="BSSID:"/>

    <TextView
        android:id="@+id/rssi_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/bssid_title"
        android:layout_alignStart="@id/bssid_title"
        android:text="RSSI:"/>

    <TextView
        android:id="@+id/rssi_value"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@id/rssi_title"
        android:layout_toEndOf="@id/rssi_title"
        android:layout_marginStart="12dp"
        android:text="rssi:"/>

</RelativeLayout>